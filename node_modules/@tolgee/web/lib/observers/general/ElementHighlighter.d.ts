import { ElementMeta, TolgeeElement } from '../../types';
type Props = {
    highlightColor: string;
    highlightWidth: number;
};
export declare function ElementHighlighter({ highlightColor, highlightWidth }: Props): Readonly<{
    initHighlighter(element: TolgeeElement, elementMeta: ElementMeta): void;
}>;
export {};
