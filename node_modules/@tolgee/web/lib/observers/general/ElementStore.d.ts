import { TolgeeElement, ElementMeta } from '../../types';
export type RegistredElementsMap = Map<TolgeeElement, ElementMeta>;
export declare function ElementStore(): Readonly<{
    set(el: TolgeeElement, meta: ElementMeta): void;
    get(el: TolgeeElement | undefined): ElementMeta;
    remove(el: TolgeeElement): boolean;
    forEachElement(callback: (el: TolgeeElement, meta: ElementMeta) => void): void;
}>;
export type ElementStoreType = ReturnType<typeof ElementStore>;
