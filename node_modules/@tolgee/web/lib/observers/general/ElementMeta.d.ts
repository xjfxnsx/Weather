import type { KeyAndParams } from '@tolgee/core';
import type { ElementMeta, NodeMeta, TolgeeElement } from '../../types';
export declare function initElementMeta(element: TolgeeElement): ElementMeta;
export declare function initNodeMeta(oldTextContent: string, keys: KeyAndParams[]): NodeMeta;
