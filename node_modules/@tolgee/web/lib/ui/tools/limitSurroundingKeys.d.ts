export type KeyInfo = {
    keyName: string;
    keyNamespace?: string;
};
export declare function limitSurroundingKeys(positions: KeyInfo[], current: KeyInfo): {
    keyName: string;
    namespace: string;
}[];
