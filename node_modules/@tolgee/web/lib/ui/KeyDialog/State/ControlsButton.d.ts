import React from 'react';
import { IconButton } from '@mui/material';
type Props = React.ComponentProps<typeof IconButton> & {
    tooltip?: React.ReactNode;
};
export declare const ControlsButton: React.FC<Props>;
export {};
