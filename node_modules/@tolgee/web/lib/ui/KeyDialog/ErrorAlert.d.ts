import { HttpError } from '../client/HttpError';
type Props = {
    error: HttpError | Error;
    severity?: 'error' | 'info';
};
export declare const ErrorAlert: ({ error, severity }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
