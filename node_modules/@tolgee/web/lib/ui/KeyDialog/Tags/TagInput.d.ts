import React from 'react';
type Props = {
    onClose?: () => void;
    onAdd: (name: string) => void;
    className?: string;
    autoFocus?: boolean;
    existing?: string[];
    placeholder?: string;
};
export declare const TagInput: React.FC<Props>;
export {};
