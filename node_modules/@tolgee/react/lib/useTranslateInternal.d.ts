import { TranslateProps, NsFallback } from '@tolgee/web';
import { ReactOptions } from './types';
export declare const useTranslateInternal: (ns?: NsFallback, options?: ReactOptions) => {
    t: (props: TranslateProps<any>) => any;
    isLoading: boolean;
};
